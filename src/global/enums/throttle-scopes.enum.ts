export enum ThrottleScopes {
  ONE_PER_MINUTE = 'one-per-minute',
  THREE_PER_MINUTE = 'three-per-minute',
  FIFTY_PER_MINUTE = 'fifty-per-minute',
  HUNDRED_PER_MINUTE = 'hundred-per-minute',
  FIVE_HUNDRED_PER_MINUTE = 'five-hundred-per-minute',
}
