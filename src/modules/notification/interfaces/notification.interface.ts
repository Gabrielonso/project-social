export enum NotificationCategoryEnum {
  ALL_USERS = 'all_users',
  SELECTED = 'selected',
  FILTERED = 'filtered',
}

export enum NotificationType {
  SCHEDULED = 'scheduled',
  INSTANT = 'instant',
}

export enum NotificationOccurence {
  ONE_TIME = 'one-time',
  RECURRING = 'recurring',
}

export enum NotificationBases {
  DAY = 'day',
  DATE = 'date',
  MONTH = 'month',
  TIME = 'time',
  YEAR = 'year',
}
